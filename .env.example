# Database (use Turso for production, local SQLite for dev)
DATABASE_URL=file:./local.db
# DATABASE_AUTH_TOKEN=  # only needed for Turso production

# Better Auth
BETTER_AUTH_SECRET=replace_with_32_char_random_string
BETTER_AUTH_URL=http://localhost:3000

# Claude AI
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: email (for magic-link auth)
# RESEND_API_KEY=re_...
# EMAIL_FROM=hello@yourdomain.com
